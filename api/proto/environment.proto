syntax = "proto3";
package lsfn;

import "api/proto/ship.proto";

message JoinServer {
}

message Welcome {
    ShipDescription ship = 1;
}

service Lobby {
  rpc Join (JoinServer) returns (Welcome) {}
}
